apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: erpnext
spec:
  selector:
    matchLabels:
      app: erpnext
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
