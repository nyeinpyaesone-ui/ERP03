apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: erp03-metrics
  namespace: erp03-monitoring
  labels:
    app.kubernetes.io/name: erp03-metrics
    app.kubernetes.io/part-of: erp03
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
    - erp03
  selector:
    matchLabels:
      app.kubernetes.io/part-of: erp03
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: erp03-alerts
  namespace: erp03-monitoring
  labels:
    app.kubernetes.io/name: erp03-alerts
    app.kubernetes.io/part-of: erp03
    release: prometheus
spec:
  groups:
  - name: erp03
    rules:
    - alert: AIERPApiHighErrorRate
      expr: |
        (
          sum(rate(http_requests_total{job=~"erp03-api",status=~"5.."}[5m]))
          /
          sum(rate(http_requests_total{job=~"erp03-api"}[5m]))
        ) > 0.05
      for: 5m
      labels:
        severity: critical
      annotations:
        summary: "ERP erp03 API High Error Rate"
        description: "Error rate is above 5% for {{ $labels.namespace }}/{{ $labels.pod }}"

    - alert: AIERPApiHighLatency
      expr: |
        histogram_quantile(0.95,
          sum(rate(http_request_duration_seconds_bucket{job=~"erp03-api"}[5m])) by (le, namespace, pod)
        ) > 2
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "ERP erp03 API High Latency"
        description: "P95 latency is above 2s for {{ $labels.namespace }}/{{ $labels.pod }}"

    - alert: AIERPApiPodRestart
      expr: |
        rate(kube_pod_container_status_restarts_total{namespace="erp03",container="api"}[15m]) > 0
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "ERP erp03 API Pod Restarting"
        description: "Pod {{ $labels.pod }} is restarting frequently"

    - alert: AIERPDBHighConnections
      expr: |
        pg_stat_activity_count{namespace="erp03"} > 150
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "ERP erp03 Database High Connections"
        description: "Database connections are above 150"

    - alert: AIERPDBReplicationLag
      expr: |
        pg_replication_lag{namespace="erp03"} > 300
      for: 5m
      labels:
        severity: critical
      annotations:
        summary: "ERP erp03 Database Replication Lag"
        description: "Replication lag is above 5 minutes"

    - alert: AIERPWorkerQueueDepth
      expr: |
        queue_depth{namespace="erp03"} > 1000
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "ERP erp03 Worker Queue Depth High"
        description: "Worker queue depth is above 1000"

    - alert: AIERPPVCUsage
      expr: |
        (
          kubelet_volume_stats_used_bytes{namespace="erp03"}
          /
          kubelet_volume_stats_capacity_bytes{namespace="erp03"}
        ) > 0.85
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "ERP erp03 PVC Usage High"
        description: "PVC {{ $labels.persistentvolumeclaim }} is above 85%"

