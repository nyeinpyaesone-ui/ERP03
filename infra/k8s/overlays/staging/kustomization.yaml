apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: erpo3-staging

resources:
  - ../base

namePrefix: staging-

commonLabels:
  environment: staging

replicas:
  - name: erpo3-api
    count: 2
  - name: erpo3-web
    count: 1
  - name: erpo3-worker
    count: 1

patches:
  - target:
      kind: ConfigMap
      name: erpo3-config
    patch: |-
      - op: replace
        path: /data/APP_ENV
        value: staging
      - op: replace
        path: /data/DB_NAME
        value: erpo3_staging
      - op: replace
        path: /data/API_BASE_URL
        value: https://staging-api.erp-domain.com
      - op: replace
        path: /data/ENABLE_PUSH_NOTIFICATIONS
        value: "false"

  - target:
      kind: Ingress
      name: erpo3-ingress
    patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: staging-api.erp-domain.com
      - op: replace
        path: /spec/rules/1/host
        value: staging-admin.erp-domain.com
      - op: replace
        path: /spec/rules/2/host
        value: staging-store.erp-domain.com
      - op: replace
        path: /spec/tls/0/hosts
        value:
          - staging-api.erp-domain.com
          - staging-admin.erp-domain.com
          - staging-store.erp-domain.com
      - op: replace
        path: /spec/tls/0/secretName
        value: staging-erpo3-tls
      - op: add
        path: /metadata/annotations/nginx.ingress.kubernetes.io~1whitelist-source-range
        value: "0.0.0.0/0"

  - target:
      kind: HorizontalPodAutoscaler
      name: erpo3-api-hpa
    patch: |-
      - op: replace
        path: /spec/minReplicas
        value: 2
      - op: replace
        path: /spec/maxReplicas
        value: 5

images:
  - name: ghcr.io/nyeinpyaesone-ui/erpo3-api
    newTag: staging
  - name: ghcr.io/nyeinpyaesone-ui/erpo3-web
    newTag: staging
  - name: ghcr.io/nyeinpyaesone-ui/erpo3-worker
    newTag: staging

