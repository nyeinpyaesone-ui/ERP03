# External Secrets Operator Configuration
# Replaces hardcoded secrets with AWS Secrets Manager references

apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: aws-secret-store
  namespace: erp-solution
spec:
  provider:
    aws:
      service: SecretsManager
      region: ap-southeast-1
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets-sa
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: erp-db-secret
  namespace: erp-solution
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: aws-secret-store
  target:
    name: erp-db-credentials
    creationPolicy: Owner
  data:
    - secretKey: DB_PASSWORD
      remoteRef:
        key: erp/dev/db-password
        property: password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: erp-redis-secret
  namespace: erp-solution
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: aws-secret-store
  target:
    name: erp-redis-credentials
    creationPolicy: Owner
  data:
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: erp/dev/redis-auth-token
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: erp-jwt-secret
  namespace: erp-solution
spec:
  refreshInterval: 24h
  secretStoreRef:
    kind: SecretStore
    name: aws-secret-store
  target:
    name: erp-jwt-credentials
    creationPolicy: Owner
  data:
    - secretKey: JWT_SECRET
      remoteRef:
        key: erp/dev/jwt-secret
