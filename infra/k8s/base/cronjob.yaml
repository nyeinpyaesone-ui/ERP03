apiVersion: batch/v1
kind: CronJob
metadata:
  name: erpo3-db-backup
  namespace: erpo3
  labels:
    app.kubernetes.io/name: erpo3-db-backup
    app.kubernetes.io/component: backup
    app.kubernetes.io/part-of: erpo3
spec:
  schedule: "0 2 * * *"
  timeZone: "Asia/Yangon"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 7
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app.kubernetes.io/name: erpo3-db-backup
        spec:
          restartPolicy: OnFailure
          containers:
          - name: backup
            image: postgres:15-alpine
            command:
            - /bin/sh
            - -c
            - |
              export PGPASSWORD=$(DB_PASSWORD)
              pg_dump -h erpo3-postgres -U $(DB_USER) -d $(DB_NAME) --clean --if-exists --verbose | gzip > /backup/erpo3-$(date +%Y%m%d-%H%M%S).sql.gz
              aws s3 cp /backup/ s3://$(S3_BUCKET)/backups/database/ --recursive --storage-class STANDARD_IA
              find /backup -name "*.sql.gz" -mtime +30 -delete
            env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: erpo3-secrets
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: erpo3-secrets
                  key: DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: erpo3-config
                  key: DB_NAME
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: erpo3-secrets
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: erpo3-secrets
                  key: AWS_SECRET_ACCESS_KEY
            - name: S3_BUCKET
              valueFrom:
                configMapKeyRef:
                  name: erpo3-config
                  key: S3_BUCKET
            volumeMounts:
            - name: backup
              mountPath: /backup
          volumes:
          - name: backup
            emptyDir:
              sizeLimit: 10Gi
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: erpo3-redis-backup
  namespace: erpo3
  labels:
    app.kubernetes.io/name: erpo3-redis-backup
    app.kubernetes.io/component: backup
    app.kubernetes.io/part-of: erpo3
spec:
  schedule: "0 3 * * *"
  timeZone: "Asia/Yangon"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 7
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app.kubernetes.io/name: erpo3-redis-backup
        spec:
          restartPolicy: OnFailure
          containers:
          - name: backup
            image: redis:7-alpine
            command:
            - /bin/sh
            - -c
            - |
              redis-cli -h erpo3-redis -a $(REDIS_PASSWORD) --rdb /backup/erpo3-redis-$(date +%Y%m%d-%H%M%S).rdb
              aws s3 cp /backup/ s3://$(S3_BUCKET)/backups/redis/ --recursive --storage-class STANDARD_IA
              find /backup -name "*.rdb" -mtime +30 -delete
            env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: erpo3-secrets
                  key: REDIS_PASSWORD
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: erpo3-secrets
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: erpo3-secrets
                  key: AWS_SECRET_ACCESS_KEY
            - name: S3_BUCKET
              valueFrom:
                configMapKeyRef:
                  name: erpo3-config
                  key: S3_BUCKET
            volumeMounts:
            - name: backup
              mountPath: /backup
          volumes:
          - name: backup
            emptyDir:
              sizeLimit: 5Gi

