# erpnext_custom

Placeholder for the organisation's custom Frappe/ERPNext application.

Add the real Frappe app source here before building a production image. The image build intentionally fails if the application assets cannot be built; no `|| true` error suppression is used.
