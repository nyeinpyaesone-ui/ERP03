# Infrastructure

Runtime and operational dependencies only: PostgreSQL, Redis/event transport, Ollama/model runtime, deployment assets, secrets/configuration, health checks, and observability.

Infrastructure must not contain ERP business rules.